---
import Layout from '../layouts/Layout.astro';

const features = [
  {
    icon: 'M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z',
    title: 'Curated Prompts',
    description: 'Every prompt in our collection is tested and refined to ensure high-quality results across different AI platforms.'
  },
  {
    icon: 'M7 21a4 4 0 01-4-4V5a2 2 0 012-2h4a2 2 0 012 2v12a4 4 0 01-4 4zm0 0h12a2 2 0 002-2v-4a2 2 0 00-2-2h-2.343M11 7.343l1.657-1.657a2 2 0 012.828 0l2.829 2.829a2 2 0 010 2.828l-8.486 8.485M7 17h.01',
    title: 'Diverse Styles',
    description: 'From minimalist designs to bold graphics, we cover a wide range of aesthetic styles to match any brand personality.'
  },
  {
    icon: 'M13 10V3L4 14h7v7l9-11h-7z',
    title: 'Easy to Use',
    description: 'Simply copy a prompt and paste it into your preferred AI image generator to start creating.'
  }
];
---

<Layout title="About Us - LogoPrompt.pro">
  <main class="page-content">
    <a href="/" class="back-link">
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
        <line x1="19" y1="12" x2="5" y2="12"></line>
        <polyline points="12 19 5 12 12 5"></polyline>
      </svg>
      Back to Gallery
    </a>

    <header class="page-header">
      <h1>About <span class="text-gradient">LogoPrompt.pro</span></h1>
      <p class="subtitle">Empowering creativity through AI-powered logo inspiration</p>
    </header>

    <section class="mission-section">
      <div class="mission-card">
        <div class="mission-icon">
          <svg xmlns="http://www.w3.org/2000/svg" width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="10"></circle>
            <path d="M8 14s1.5 2 4 2 4-2 4-2"></path>
            <line x1="9" y1="9" x2="9.01" y2="9"></line>
            <line x1="15" y1="9" x2="15.01" y2="9"></line>
          </svg>
        </div>
        <h2>Our Mission</h2>
        <p>
          At LogoPrompt.pro, we believe that great design should be accessible to everyone. Our mission is to
          democratize logo creation by providing high-quality AI prompts that inspire creativity and help
          bring your brand vision to life.
        </p>
      </div>
    </section>

    <section class="features-section">
      <h2 class="section-title">What We Offer</h2>
      <p class="section-description">
        We curate and create carefully crafted prompts designed specifically for AI image generators like
        Ideogram, Google Gemini, and other leading platforms.
      </p>
      <div class="features-grid">
        {features.map(feature => (
          <div class="feature-card">
            <div class="feature-icon">
              <svg xmlns="http://www.w3.org/2000/svg" width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
                <path d={feature.icon}></path>
              </svg>
            </div>
            <h3>{feature.title}</h3>
            <p>{feature.description}</p>
          </div>
        ))}
      </div>
    </section>

    <section class="content-section">
      <h2>Why AI Logo Prompts?</h2>
      <p>
        AI-powered design tools have revolutionized the creative industry. With the right prompts,
        anyone can generate stunning logo concepts in seconds. We bridge the gap between complex AI
        technology and everyday users by providing ready-to-use prompts that deliver consistent,
        professional results.
      </p>
    </section>

    <section class="content-section">
      <h2>Our Team</h2>
      <p>
        LogoPrompt.pro is built by a passionate team of designers, developers, and AI enthusiasts
        who understand both the creative process and the technical aspects of AI image generation.
        We're constantly exploring new techniques and updating our prompt library to stay at the
        forefront of AI-driven design.
      </p>
    </section>

    <section class="cta-section">
      <h2>Ready to Get Started?</h2>
      <p>Browse our collection of AI logo prompts and start creating today.</p>
      <a href="/" class="cta-button">
        <svg xmlns="http://www.w3.org/2000/svg" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <polygon points="13 2 3 14 12 14 11 22 21 10 12 10 13 2"></polygon>
        </svg>
        Explore Gallery
      </a>
    </section>
  </main>
</Layout>

<style>
  .page-content {
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem 1.5rem 4rem;
  }

  .back-link {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    color: rgb(var(--text-secondary));
    text-decoration: none;
    font-size: 0.9rem;
    font-weight: 500;
    margin-bottom: 2.5rem;
    transition: all var(--transition-fast);
  }

  .back-link:hover {
    color: rgb(var(--accent));
    transform: translateX(-4px);
  }

  .page-header {
    text-align: center;
    margin-bottom: 3rem;
  }

  h1 {
    font-size: clamp(2rem, 5vw, 3rem);
    font-weight: 800;
    margin: 0 0 1rem;
    letter-spacing: -0.02em;
    color: rgb(var(--text-primary));
  }

  .text-gradient {
    color: rgb(var(--accent));
  }

  .subtitle {
    font-size: 1.1rem;
    color: rgb(var(--text-secondary));
    margin: 0;
  }

  /* Mission Section */
  .mission-section {
    margin-bottom: 4rem;
  }

  .mission-card {
    text-align: center;
    padding: 3rem 2rem;
    background: rgb(var(--card-bg));
    border: 1px solid rgb(var(--card-border));
    border-radius: var(--radius-xl);
    box-shadow: var(--shadow-sm);
  }

  .mission-icon {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    width: 64px;
    height: 64px;
    background: rgb(var(--accent-light));
    border: 1px solid rgba(var(--accent), 0.2);
    border-radius: var(--radius-lg);
    color: rgb(var(--accent));
    margin-bottom: 1.5rem;
  }

  .mission-card h2 {
    font-size: 1.5rem;
    font-weight: 700;
    margin: 0 0 1rem;
    color: rgb(var(--text-primary));
  }

  .mission-card p {
    font-size: 1.05rem;
    color: rgb(var(--text-secondary));
    line-height: 1.7;
    margin: 0;
    max-width: 600px;
    margin: 0 auto;
  }

  /* Features Section */
  .features-section {
    margin-bottom: 4rem;
  }

  .section-title {
    font-size: 1.75rem;
    font-weight: 700;
    color: rgb(var(--text-primary));
    margin: 0 0 0.5rem;
    text-align: center;
  }

  .section-description {
    text-align: center;
    color: rgb(var(--text-secondary));
    margin: 0 0 2rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
  }

  .features-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.5rem;
  }

  .feature-card {
    padding: 1.5rem;
    background: rgb(var(--card-bg));
    border: 1px solid rgb(var(--card-border));
    border-radius: var(--radius-lg);
    transition: all var(--transition-base);
    box-shadow: var(--shadow-sm);
  }

  .feature-card:hover {
    transform: translateY(-4px);
    border-color: rgba(var(--accent), 0.3);
    box-shadow: var(--shadow-md);
  }

  .feature-icon {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 48px;
    height: 48px;
    background: rgb(var(--accent-light));
    border-radius: var(--radius-md);
    color: rgb(var(--accent));
    margin-bottom: 1rem;
  }

  .feature-card h3 {
    font-size: 1rem;
    font-weight: 600;
    color: rgb(var(--text-primary));
    margin: 0 0 0.5rem;
  }

  .feature-card p {
    font-size: 0.875rem;
    color: rgb(var(--text-secondary));
    line-height: 1.6;
    margin: 0;
  }

  /* Content Sections */
  .content-section {
    margin-bottom: 2.5rem;
    padding: 2rem;
    background: rgb(var(--card-bg));
    border: 1px solid rgb(var(--card-border));
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-sm);
  }

  .content-section h2 {
    font-size: 1.25rem;
    font-weight: 600;
    color: rgb(var(--text-primary));
    margin: 0 0 1rem;
  }

  .content-section p {
    color: rgb(var(--text-secondary));
    line-height: 1.7;
    margin: 0;
  }

  /* CTA Section */
  .cta-section {
    text-align: center;
    padding: 3rem 2rem;
    background: rgb(var(--accent-light));
    border: 1px solid rgba(var(--accent), 0.2);
    border-radius: var(--radius-xl);
    margin-top: 3rem;
  }

  .cta-section h2 {
    font-size: 1.5rem;
    font-weight: 700;
    color: rgb(var(--text-primary));
    margin: 0 0 0.5rem;
  }

  .cta-section p {
    color: rgb(var(--text-secondary));
    margin: 0 0 1.5rem;
  }

  .cta-button {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 0.875rem 1.75rem;
    background: rgb(var(--accent));
    border: none;
    border-radius: var(--radius-sm);
    color: white;
    font-size: 1rem;
    font-weight: 600;
    text-decoration: none;
    transition: all var(--transition-fast);
  }

  .cta-button:hover {
    background: rgb(var(--accent-hover));
    transform: translateY(-2px);
    box-shadow: 0 8px 24px rgba(var(--accent), 0.3);
  }

  @media (max-width: 768px) {
    .features-grid {
      grid-template-columns: 1fr;
    }

    .mission-card,
    .content-section,
    .cta-section {
      padding: 2rem 1.5rem;
    }
  }
</style>
